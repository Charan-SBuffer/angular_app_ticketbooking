import { VwBusCompleteData } from './vw-bus-complete-data';

describe('VwBusCompleteData', () => {
  it('should create an instance', () => {
    expect(new VwBusCompleteData()).toBeTruthy();
  });
});
